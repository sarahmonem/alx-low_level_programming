#include <stdio.h>
#include "main.h"

/**
 * main- entry point
 * 
 * Description: print _isupper
 *
 *
 *
 * Return: Alway 0 (success)
 */

int _isupper(int ch)
{
	if (isupper(ch))
	{
		return(1);
	}
	else
	{
		return(0);
	}
	return (0)
}
